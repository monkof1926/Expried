



<MNavigationBar>:
    # panel_color: 0,0,1,1
    # text_color_normal: 1,1,0,1
    # text_color_active: 1,1,0,1
    MDBottomNavigationItem:
        # canvas:
        #     Color:
        #         rgba: [.2,.2,.2,1]
        #     Rectangle:
        #         size: root.size
        #         pos: root.pos
        name: 'menu'
        text: 'Menu'
        icon: 'backburger'
        # on_tab_press: root.manager.current = 'menu'
        # on_tab_press: app.sm.current = 'menu'


    MDBottomNavigationItem:
        canvas:
            Color:
                rgba: [.2,.2,.2,1]
            Rectangle:
                size: root.size
                pos: root.pos
        name: 'list'
        text: 'List'
        icon: 'format-list-numbered'
        on_tab_press: app.sm.current = 'list'
        # on_tab_press: root.manager.current = 'list'

    MDBottomNavigationItem:
        canvas:
            Color:
                rgba: [.2,.2,.2,1]
            Rectangle:
                size: root.size
                pos: root.pos
        name: 'scan'
        text: 'Scan'
        icon: 'camera'
        on_tab_press: app.sm.current = 'scan'

    MDBottomNavigationItem:
        canvas:
            Color:
                rgba: [.2,.2,.2,1]
            Rectangle:
                size: root.size
                pos: root.pos
        name: 'settings'
        text: 'Settings'
        icon: 'cog'
        # on_tab_press: root.manager.current = 'settings'
        on_tab_press: app.sm.current = 'settings'

<Toolbar>:
    title: "Scan Screen"
    pos_hint: {"top": 1}
    anchor_title: 'center'
    right_action_items: [["close-circle", lambda x: app.stop()]]
